using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterAvatarSystem : MonoBehaviour
{

    public Transform head, left_hand, right_hand, left_foot, right_foot, body;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
